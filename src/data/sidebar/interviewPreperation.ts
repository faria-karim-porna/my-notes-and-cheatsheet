const html = {
  view: "HTML_INTERVIEW_PREPARATION",
  outer: "Html",
  inner: [],
};

const css = {
  view: "CSS_INTERVIEW_PREPARATION",
  outer: "Css",
  inner: [],
};

const bootstrap = {
  view: "BOOTSTRAP_INTERVIEW_PREPARATION",
  outer: "Bootstrap",
  inner: [],
};

const reactJS = {
  view: "REACT_JS_INTERVIEW_PREPARATION",
  outer: "React.js",
  inner: [],
};

const angularJS = {
  view: "ANGULAR_JS_INTERVIEW_PREPARATION",
  outer: "Angular.js",
  inner: [],
};

const frontendDevelopment = {
  view: "FRONTEND_DEVELOPMENT_INTERVIEW_PREPARATION",
  outer: "Frontend Development",
  inner: [html, css, bootstrap, reactJS, angularJS],
};

const nodeJS = {
  view: "NODE_JS_INTERVIEW_PREPARATION",
  outer: "Node.js",
  inner: [],
};

const django = {
  view: "DJANGO_INTERVIEW_PREPARATION",
  outer: "Django",
  inner: [],
};

const flask = {
  view: "FLASK_INTERVIEW_PREPARATION",
  outer: "Flask",
  inner: [],
};

const laravel = {
  view: "LARAVEL_INTERVIEW_PREPARATION",
  outer: "Laravel",
  inner: [],
};

const dotNet = {
  view: "DOT_NET_INTERVIEW_PREPARATION",
  outer: ".NET",
  inner: [],
};

const backendDevelopment = {
  view: "BACKEND_DEVELOPMENT_INTERVIEW_PREPARATION",
  outer: "Backend Development",
  inner: [nodeJS, django, flask, laravel, dotNet],
};

const javascript = {
  view: "JAVASCRIPT_INTERVIEW_PREPARATION",
  outer: "JavaScript",
  inner: [],
};

const typescript = {
  view: "TYPESCRIPT_INTERVIEW_PREPARATION",
  outer: "TypeScript",
  inner: [],
};
const cPlusPlus = {
  view: "C_PLUS_PLUS_INTERVIEW_PREPARATION",
  outer: "C++",
  inner: [],
};
const c = {
  view: "C_INTERVIEW_PREPARATION",
  outer: "C",
  inner: [],
};
const cSharp = {
  view: "C_SHARP_INTERVIEW_PREPARATION",
  outer: "C#",
  inner: [],
};
const php = {
  view: "PHP_INTERVIEW_PREPARATION",
  outer: "Php",
  inner: [],
};
const python = {
  view: "Python_INTERVIEW_PREPARATION",
  outer: "Python",
  inner: [],
};

const programmingLanguages = {
  view: "PROGRAMMING_LANGUAGES_INTERVIEW_PREPARATION",
  outer: "Programming Languages",
  inner: [javascript, typescript, cPlusPlus, c, cSharp, php, python],
};

const basicAi = {
  view: "BASIC_AI_INTERVIEW_PREPARATION",
  outer: "Basic AI",
  inner: [],
};

const machineLearning = {
  view: "MACHINE_LEARNING_INTERVIEW_PREPARATION",
  outer: "Machine Learning",
  inner: [],
};

const deepLearning = {
  view: "DEEP_LEARNING_INTERVIEW_PREPARATION",
  outer: "Deep Learning",
  inner: [],
};

const computerVision = {
  view: "COMPUTER_VISION_INTERVIEW_PREPARATION",
  outer: "Computer Vision",
  inner: [],
};

const natuaralLanguageProcessing = {
  view: "NATURAL_LANGUAGE_PROCESSING_INTERVIEW_PREPARATION",
  outer: "Natural Language Processing",
  inner: [],
};

const aiTheory = {
  view: "AI_THEORY_INTERVIEW_PREPARATION",
  outer: "Theory",
  inner: [basicAi, machineLearning, deepLearning, computerVision, natuaralLanguageProcessing],
};

const matplotlib = {
  view: "MATPLOTLIB_INTERVIEW_PREPARATION",
  outer: "Matplotlib",
  inner: [],
};

const keras = {
  view: "KERAS_INTERVIEW_PREPARATION",
  outer: "Keras",
  inner: [],
};

const opencv = {
  view: "OPENCV_INTERVIEW_PREPARATION",
  outer: "OpenCV",
  inner: [],
};

const tensorflow = {
  view: "TENSORFLOW_INTERVIEW_PREPARATION",
  outer: "Tensorflow",
  inner: [],
};

const pytorch = {
  view: "PYTORCH_INTERVIEW_PREPARATION",
  outer: "Pytorch",
  inner: [],
};

const aiTools = {
  view: "AI_TOOLS_INTERVIEW_PREPARATION",
  outer: "Tools",
  inner: [matplotlib, keras, opencv, tensorflow, pytorch],
};

const artificialIntelligence = {
  view: "ARTIFICIAL_INTELLIGENCE_INTERVIEW_PREPARATION",
  outer: "Artificial Intelligence",
  inner: [aiTheory, aiTools],
};

const unity = {
  view: "UNITY_INTERVIEW_PREPARATION",
  outer: "Unity",
  inner: [],
};

const htmlCssJs = {
  view: "HTML_CSS_JS_INTERVIEW_PREPARATION",
  outer: "HTML CSS JS",
  inner: [],
};

const gameDevelopment = {
  view: "GAME_DEVELOPMENT_INTERVIEW_PREPARATION",
  outer: "Game Development",
  inner: [unity, htmlCssJs],
};

const flutter = {
  view: "FLUTTER_INTERVIEW_PREPARATION",
  outer: "Flutter",
  inner: [],
};

const ionic = {
  view: "IONIC_INTERVIEW_PREPARATION",
  outer: "Ionic",
  inner: [],
};

const reactNative = {
  view: "REACT_NATIVE_INTERVIEW_PREPARATION",
  outer: "React Native",
  inner: [],
};

const mobileAppDevelopment = {
  view: "MOBILE_APP_DEVELOPMENT_INTERVIEW_PREPARATION",
  outer: "Mobile App Development",
  inner: [flutter, ionic, reactNative],
};

const electronJS = {
  view: "ELECTRON_JS_INTERVIEW_PREPARATION",
  outer: "Electron.js",
  inner: [],
};

const desktopAppDevelopment = {
  view: "DESKTOP_APP_DEVELOPMENT_INTERVIEW_PREPARATION",
  outer: "Desktop App Development",
  inner: [electronJS],
};

const designPatterns = {
  view: "DESIGN_PATTERNS_INTERVIEW_PREPARATION",
  outer: "Design Patterns",
  inner: [],
};

const oops = {
  view: "OOPS_INTERVIEW_PREPARATION",
  outer: "Object Oriented Programming",
  inner: [],
};

const dbms = {
  view: "DBMS_INTERVIEW_PREPARATION",
  outer: "DBMS",
  inner: [],
};

const computerNetworking = {
  view: "COMPUTER_NETWORKING_INTERVIEW_PREPARATION",
  outer: "Computer Networking",
  inner: [],
};

const computerArchitecture = {
  view: "COMPUTER_ARCHITECTURE_INTERVIEW_PREPARATION",
  outer: "Computer Architecture",
  inner: [],
};

const regex = {
  view: "REGEX_INTERVIEW_PREPARATION",
  outer: "Regex",
  inner: [],
};

const behavioral = {
  view: "BEHAVIORAL_INTERVIEW_PREPARATION",
  outer: "Behavioral Questions",
  inner: [],
};

const companyRelated = {
  view: "COMPANY_RELATED_INTERVIEW_PREPARATION",
  outer: "Company Related Questions",
  inner: [],
};

const others = {
  view: "OTHERS_INTERVIEW_PREPARATION",
  outer: "Others",
  inner: [designPatterns, oops, dbms, computerNetworking, computerArchitecture, regex, behavioral, companyRelated],
};

const blockchain = {
  view: "BLOCKCHAIN_INTERVIEW_PREPARATION",
  outer: "Blockchain",
  inner: [],
};

const ethicalHacking = {
  view: "ETHICAL_HACKING_INTERVIEW_PREPARATION",
  outer: "Ethical Hacking",
  inner: [],
};

const dataStructureAndAlgorithms = {
  view: "DATA_STRUCTURES_AND_ALGORITHMS_INTERVIEW_PREPARATION",
  outer: "Data Structure And Algorithms",
  inner: [],
};

export const interviewPreparation = {
  view: "INTERVIEW_PREPARATION",
  outer: "Interview Preparation",
  inner: [
    dataStructureAndAlgorithms,
    programmingLanguages,
    frontendDevelopment,
    backendDevelopment,
    mobileAppDevelopment,
    desktopAppDevelopment,
    gameDevelopment,
    artificialIntelligence,
    blockchain,
    ethicalHacking,
    others,
  ],
};
