const html = {
  view: "HTML_CHEATSHEET",
  outer: "Html",
  inner: [],
};

const css = {
  view: "CSS_CHEATSHEET",
  outer: "Css",
  inner: [],
};

const bootstrap = {
  view: "BOOTSTRAP_CHEATSHEET",
  outer: "Bootstrap",
  inner: [],
};

const reactJS = {
  view: "REACT_JS_CHEATSHEET",
  outer: "React.js",
  inner: [],
};

const angularJS = {
  view: "ANGULAR_JS_CHEATSHEET",
  outer: "Angular.js",
  inner: [],
};

const frontendDevelopment = {
  view: "FRONTEND_DEVELOPMENT_CHEATSHEET",
  outer: "Frontend Development",
  inner: [html, css, bootstrap, reactJS, angularJS],
};

const nodeJS = {
  view: "NODE_JS_CHEATSHEET",
  outer: "Node.js",
  inner: [],
};

const django = {
  view: "DJANGO_CHEATSHEET",
  outer: "Django",
  inner: [],
};

const flask = {
  view: "FLASK_CHEATSHEET",
  outer: "Flask",
  inner: [],
};

const laravel = {
  view: "LARAVEL_CHEATSHEET",
  outer: "Laravel",
  inner: [],
};

const dotNet = {
  view: "DOT_NET_CHEATSHEET",
  outer: ".NET",
  inner: [],
};

const backendDevelopment = {
  view: "BACKEND_DEVELOPMENT_CHEATSHEET",
  outer: "Backend Development",
  inner: [nodeJS, django, flask, laravel, dotNet],
};

const javascript = {
  view: "JAVASCRIPT_CHEATSHEET",
  outer: "JavaScript",
  inner: [],
};

const typescript = {
  view: "TYPESCRIPT_CHEATSHEET",
  outer: "TypeScript",
  inner: [],
};
const cPlusPlus = {
  view: "C_PLUS_PLUS_CHEATSHEET",
  outer: "C++",
  inner: [],
};
const c = {
  view: "C_CHEATSHEET",
  outer: "C",
  inner: [],
};
const cSharp = {
  view: "C_SHARP_CHEATSHEET",
  outer: "C#",
  inner: [],
};
const php = {
  view: "PHP_CHEATSHEET",
  outer: "Php",
  inner: [],
};
const python = {
  view: "Python_CHEATSHEET",
  outer: "Python",
  inner: [],
};

const programmingLanguages = {
  view: "PROGRAMMING_LANGUAGES_CHEATSHEET",
  outer: "Programming Languages",
  inner: [javascript, typescript, cPlusPlus, c, cSharp, php, python],
};

const basicAi = {
  view: "BASIC_AI_CHEATSHEET",
  outer: "Basic AI",
  inner: [],
};

const machineLearning = {
  view: "MACHINE_LEARNING_CHEATSHEET",
  outer: "Machine Learning",
  inner: [],
};

const deepLearning = {
  view: "DEEP_LEARNING_CHEATSHEET",
  outer: "Deep Learning",
  inner: [],
};

const computerVision = {
  view: "COMPUTER_VISION_CHEATSHEET",
  outer: "Computer Vision",
  inner: [],
};

const natuaralLanguageProcessing = {
  view: "NATURAL_LANGUAGE_PROCESSING_CHEATSHEET",
  outer: "Natural Language Processing",
  inner: [],
};

const aiTheory = {
  view: "AI_THEORY_CHEATSHEET",
  outer: "Theory",
  inner: [basicAi, machineLearning, deepLearning, computerVision, natuaralLanguageProcessing],
};

const matplotlib = {
  view: "MATPLOTLIB_CHEATSHEET",
  outer: "Matplotlib",
  inner: [],
};

const keras = {
  view: "KERAS_CHEATSHEET",
  outer: "Keras",
  inner: [],
};

const opencv = {
  view: "OPENCV_CHEATSHEET",
  outer: "OpenCV",
  inner: [],
};

const tensorflow = {
  view: "TENSORFLOW_CHEATSHEET",
  outer: "Tensorflow",
  inner: [],
};

const pytorch = {
  view: "PYTORCH_CHEATSHEET",
  outer: "Pytorch",
  inner: [],
};

const aiTools = {
  view: "AI_TOOLS_CHEATSHEET",
  outer: "Tools",
  inner: [matplotlib, keras, opencv, tensorflow, pytorch],
};

const artificialIntelligence = {
  view: "ARTIFICIAL_INTELLIGENCE_CHEATSHEET",
  outer: "Artificial Intelligence",
  inner: [aiTheory, aiTools],
};

const unity = {
  view: "UNITY_CHEATSHEET",
  outer: "Unity",
  inner: [],
};

const htmlCssJs = {
  view: "HTML_CSS_JS_CHEATSHEET",
  outer: "HTML CSS JS",
  inner: [],
};

const gameDevelopment = {
  view: "GAME_DEVELOPMENT_CHEATSHEET",
  outer: "Game Development",
  inner: [unity, htmlCssJs],
};

const flutter = {
  view: "FLUTTER_CHEATSHEET",
  outer: "Flutter",
  inner: [],
};

const ionic = {
  view: "IONIC_CHEATSHEET",
  outer: "Ionic",
  inner: [],
};

const reactNative = {
  view: "REACT_NATIVE_CHEATSHEET",
  outer: "React Native",
  inner: [],
};

const mobileAppDevelopment = {
  view: "MOBILE_APP_DEVELOPMENT_CHEATSHEET",
  outer: "Mobile App Development",
  inner: [flutter, ionic, reactNative],
};

const electronJS = {
  view: "ELECTRON_JS_CHEATSHEET",
  outer: "Electron.js",
  inner: [],
};

const desktopAppDevelopment = {
  view: "DESKTOP_APP_DEVELOPMENT_CHEATSHEET",
  outer: "Desktop App Development",
  inner: [electronJS],
};

const designPatterns = {
  view: "DESIGN_PATTERNS_CHEATSHEET",
  outer: "Design Patterns",
  inner: [],
};

const oops = {
  view: "OOPS_CHEATSHEET",
  outer: "Object Oriented Programming",
  inner: [],
};

const dbms = {
  view: "DBMS_CHEATSHEET",
  outer: "DBMS",
  inner: [],
};

const computerNetworking = {
  view: "COMPUTER_NETWORKING_CHEATSHEET",
  outer: "Computer Networking",
  inner: [],
};

const computerArchitecture = {
  view: "COMPUTER_ARCHITECTURE_CHEATSHEET",
  outer: "Computer Architecture",
  inner: [],
};

const regex = {
  view: "REGEX_CHEATSHEET",
  outer: "Regex",
  inner: [],
};

const spokenEnglish = {
  view: "SPOKEN_ENGLISH_CHEATSHEET",
  outer: "Spoken English",
  inner: [],
};

const others = {
  view: "OTHERS_CHEATSHEET",
  outer: "Others",
  inner: [designPatterns, oops, dbms, computerNetworking, computerArchitecture, regex, spokenEnglish],
};

const blockchain = {
  view: "BLOCKCHAIN_CHEATSHEET",
  outer: "Blockchain",
  inner: [],
};

const ethicalHacking = {
  view: "ETHICAL_HACKING_CHEATSHEET",
  outer: "Ethical Hacking",
  inner: [],
};

const dataStructureAndAlgorithms = {
  view: "DATA_STRUCTURES_AND_ALGORITHMS_CHEATSHEET",
  outer: "Data Structure And Algorithms",
  inner: [],
};

export const cheatsheets = {
  view: "CHEATSHEETS",
  outer: "Cheatsheets",
  inner: [
    dataStructureAndAlgorithms,
    programmingLanguages,
    frontendDevelopment,
    backendDevelopment,
    mobileAppDevelopment,
    desktopAppDevelopment,
    gameDevelopment,
    artificialIntelligence,
    blockchain,
    ethicalHacking,
    others,
  ],
};
